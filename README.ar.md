```markdown
# دليل استخدام وإدارة نظام CRM للمشاريع

## نظرة عامة

هذا المستند يقدم دليلاً شاملاً لكيفية إعداد، استخدام، وإدارة نظام إدارة علاقات العملاء (CRM) وتنسيق المشاريع. النظام مصمم لمساعدتك في عرض أعمالك، إدارة المشاريع، والتواصل مع العملاء بكفاءة.

### التقنيات المستخدمة
- **الواجهة الخلفية (Backend):** Laravel Framework
- **الواجهة الأمامية (Frontend):** Vue.js 3 with Vite
- **قاعدة البيانات:** MySQL
- **التصميم:** TailwindCSS

---

## ⚙️ الإعداد والتشغيل المحلي (Local Setup)

لتشغيل المشروع على جهازك المحلي، اتبع الخطوات التالية:

### 1. المتطلبات الأساسية
- PHP >= 8.1
- Composer
- Node.js & npm
- خادم محلي (مثل XAMPP, Laragon)

### 2. خطوات التثبيت

1.  **نسخ المستودع:**
    ```bash
    git clone <your-repository-url>
    cd crm
    ```

2.  **تثبيت الاعتماديات:**
    ```bash
    # تثبيت اعتماديات Laravel
    composer install

    # تثبيت اعتماديات Vue.js
    npm install
    ```

3.  **إعداد ملف البيئة (`.env`):**
    - قم بنسخ ملف `.env.example` إلى `.env`.
    - قم بإنشاء قاعدة بيانات جديدة (e.g., `crm_db`).
    - قم بتحديث إعدادات قاعدة البيانات في ملف `.env`:
      ```
      DB_CONNECTION=mysql
      DB_HOST=127.0.0.1
      DB_PORT=3306
      DB_DATABASE=crm_db
      DB_USERNAME=root
      DB_PASSWORD=
      ```

4.  **تهيئة التطبيق:**
    ```bash
    # إنشاء مفتاح التطبيق
    php artisan key:generate

    # تشغيل الهجرات (Migrations) لإنشاء جداول قاعدة البيانات
    php artisan migrate

    # (اختياري) تشغيل الـ Seeders لملء قاعدة البيانات ببيانات أولية
    php artisan db:seed

    # ربط مجلد التخزين
    php artisan storage:link
    ```

5.  **تشغيل المشروع:**
    - في نافذة terminal جديدة، قم بتشغيل خادم الواجهة الأمامية:
      ```bash
      npm run dev
      ```
    - في نافذة terminal أخرى، قم بتشغيل خادم Laravel:
      ```bash
      php artisan serve
      ```

- الآن يمكنك الوصول للمشروع عبر الرابط: `http://127.0.0.1:8000`
- يمكنك الوصول للوحة التحكم عبر: `http://127.0.0.1:8000/admin`

---

## 🎛️ لوحة التحكم (Admin Panel)

لوحة التحكم هي المركز الرئيسي لإدارة جميع محتويات الموقع. للوصول إليها، استخدم بيانات الدخول التالية:
- **الرابط:** `/admin`
- **البريد الإلكتروني:** `admin@crm.com`
- **كلمة المرور:** `password`

### الأقسام الرئيسية:

1.  **إدارة معرض الأعمال (Portfolio):**
    - **إضافة مشروع جديد:**
        1.  من القائمة الجانبية، اذهب إلى "معرض الأعمال".
        2.  انقر على زر "إضافة جديد".
        3.  املأ الحقول: العنوان، الوصف، العميل، تاريخ المشروع، والموقع.
        4.  ارفع الصور ومقاطع الفيديو الخاصة بالمشروع. يمكنك سحب وإفلات الملفات مباشرة.
        5.  النظام سيقوم تلقائياً بتحسين الصور (تحويلها إلى `webp` وضغطها) لضمان سرعة تحميل الموقع.
        6.  انقر على "حفظ" لنشر المشروع.

2.  **إدارة آراء العملاء (Testimonials):**
    - إضافة وتعديل شهادات العملاء التي تظهر في الصفحة الرئيسية.
    - يمكنك رفع صورة العميل مع الشهادة.

3.  **إدارة الأقسام (Sections):**
    - التحكم في محتوى الأقسام المختلفة في الصفحة الرئيسية، مثل قسم "من نحن" و "خدماتنا".

4.  **إعدادات الموقع (Settings):**
    - تحديث شعار الموقع، عنوان الموقع، والوصف.
    - التحكم في معلومات التواصل التي تظهر في التذييل (Footer) وروابط وسائل التواصل الاجتماعي.


---

## 🚀 نشر المشروع (Deployment)

لنشر التحديثات على الخادم، اتبع الخطوات التالية:

1.  **بناء ملفات الواجهة الأمامية:**
    - قبل رفع التغييرات، يجب بناء ملفات Vue.js النهائية.
    ```bash
    npm run build
    ```
    - هذا الأمر سيقوم بإنشاء مجلد `build` داخل `public` يحتوي على جميع ملفات CSS و JavaScript المضغوطة.

2.  **رفع الملفات إلى الخادم:**
    - استخدم عميل SFTP (مثل FileZilla) أو `scp` لرفع الملفات. مثال باستخدام `scp`:

      ```bash
      # رفع مجلد build
      scp -P 65002 -r public/build u737245625@82.198.228.204:domains/hazem-abdullah.com/public/

      # رفع الصور الجديدة في معرض الأعمال
      scp -P 65002 -r public/storage/portfolio u737245625@82.198.228.204:domains/hazem-abdullah.com/public/storage/
      ```

      **ملاحظة:** استبدل `65002` برقم المنفذ الصحيح و `u737245625@82.198.228.204` ببيانات اعتماد الخادم الخاصة بك.

3.  **تنفيذ أوامر على الخادم:**
    - بعد رفع الملفات، قم بالاتصال بالخادم عبر SSH ونفذ الأوامر التالية لمسح الكاش وتطبيق التغييرات:
    ```bash
    cd domains/yourdomain.com
    php artisan cache:clear
    php artisan config:clear
    php artisan view:clear
    ```

### ملاحظات هامة:
- **الصور والفيديوهات:** يتم رفعها إلى `storage/app/public` والخادم يقوم بعرضها عبر الرابط الرمزي (symlink) في `public/storage`.
- **سرعة الموقع:** تم تطبيق أفضل الممارسات لضمان سرعة الموقع، بما في ذلك تحسين الصور تلقائياً وضغط ملفات الواجهة الأمامية.

```
